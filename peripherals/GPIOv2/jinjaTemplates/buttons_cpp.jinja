{% include template_dir + "/header_cpp_file.jinja" %}

#include "distortos/board/buttons.hpp"

#ifdef CONFIG_BOARD_BUTTONS_ENABLE

#include "distortos/chip/ChipInputPin.hpp"

namespace distortos
{

namespace board
{

/*---------------------------------------------------------------------------------------------------------------------+
| global objects
+---------------------------------------------------------------------------------------------------------------------*/

const chip::ChipInputPin buttons[totalButtons]
{
{% for item in buttons_pins %} 
	/// configuration for button {% filter upper %}{{buttons_ids[loop.index - 1]}}{% endfilter %}
	chip::ChipInputPin{ chip::Pin::{{item["pin"]}}, chip::PinPull::{{item["pinPull"]}}, {% filter lower %}{{item["inverted"]}}{% endfilter %} }, 
{% endfor %}
};

}	// namespace board

}	// namespace distortos

#endif	// def CONFIG_BOARD_BUTTONS_ENABLE
{{''}}
